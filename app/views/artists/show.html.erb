<h1>Artist Info</h1>

<div>
    <h3><%= @artist.name %></h3>
    <ul>
        <% @artist.songs.each do |song| %>
            <li><%= link_to song.title, song_path(song) %></i>
        <% end %>
    </ul>
</div>